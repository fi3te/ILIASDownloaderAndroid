<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.github.fi3te.iliasdownloader" >

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:largeHeap="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity
            android:name=".view.IliasActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".view.LoginActivity"
            android:label="@string/login"
            android:launchMode="singleTop" />

        <activity
            android:name=".view.BlockedFilesActivity"
            android:label="@string/ignore_list"
            android:launchMode="singleTop" />

        <activity
            android:name=".view.LicensesActivity"
            android:label="@string/licences"
            android:launchMode="singleTop" />

        <activity
            android:name=".view.preferences.SettingsActivity"
            android:label="@string/settings"
            android:launchMode="singleTop" />

    </application>

</manifest>
